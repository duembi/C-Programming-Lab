#include<stdio.h>

int main(){
	
	int i, x;
	
	printf("Please enter a number between 1 and 10: ");
	scanf("%d", &x);
	if(x > 10 || x < 1){
			printf("Invalid input.");
			return 0;
		}
	printf("Printing numbers from 1 to %d: ", x);

	
	for(i=0; i<x; i++){
		
		printf("%d ", i+1);
		
	}
	
}
